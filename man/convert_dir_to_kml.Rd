% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gistools.R
\name{convert_dir_to_kml}
\alias{convert_dir_to_kml}
\title{Convert GPX Files to KML with Labeled Points and Trails (Auto-Timezone Detection)}
\usage{
convert_dir_to_kml(
  trip_folder,
  hover_labels = FALSE,
  point_frequency = 1,
  line_color = "ff0000ff",
  line_width = 3,
  point_scale = 0.6,
  label_scale = 0.6
)
}
\arguments{
\item{trip_folder}{A character path to a folder containing one or more \code{.gpx} files.}

\item{hover_labels}{If TRUE, labels only appear on hover/click (default: FALSE - always visible)}

\item{point_frequency}{Show every Nth trackpoint (default: 1 - all points)}

\item{line_color}{KML color code for track lines (default: "ff0000ff" - red)}

\item{line_width}{Width of track lines in pixels (default: 3)}

\item{point_scale}{Scale factor for point icons (default: 0.6)}

\item{label_scale}{Scale factor for labels (default: 0.6)}
}
\value{
Writes \code{.kml} files and optionally a combined \code{all_tracks.kml} into a \verb{kml/} subfolder.
}
\description{
This function processes all \code{.gpx} files in a given trip folder and converts
them into \code{.kml} files. Each output KML file includes:
\itemize{
\item A styled \code{LineString} showing the GPS trail
\item Individual timestamped \code{Point} placemarks with optional local time labels
}
}
\details{
The function automatically detects the local timezone based on GPS coordinates
using the \code{lutz} package, eliminating the need for manual timezone configuration.
For multi-timezone trips, it uses the timezone of the first trackpoint.

The function also generates a combined \code{all_tracks.kml} file for the trip folder
when multiple GPX files are present.
}
