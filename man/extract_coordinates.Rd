% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gistools.R
\name{extract_coordinates}
\alias{extract_coordinates}
\title{Geoencode records with GPX using per-record UTC offsets}
\usage{
extract_coordinates(
  records,
  time_coords,
  time_zone,
  max_gap_mins = 15,
  elevation_col = "elevation_in_meters",
  gps_elevation_col = "elevation_in_meters",
  overwrite_latlon_only_if_na = FALSE,
  overwrite_elev_only_if_na = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{records}{Data frame with event_date, event_time, decimal_latitude, decimal_longitude, and timezone columns}

\item{time_coords}{GPS coordinates with UTC timestamps (data.frame or CSV path).
Must have columns: date_time, decimal_latitude, decimal_longitude. Elevation column optional.}

\item{time_zone}{Column name in \code{records} containing UTC offsets per record (e.g., "UTC-7", "UTC+5.5")}

\item{max_gap_mins}{Maximum gap in minutes between record and nearest GPX point (default: 15)}

\item{elevation_col}{Name of the elevation column to write in \code{records} (default: "elevation_in_meters")}

\item{gps_elevation_col}{Name of the elevation column in the GPX data (default: "elevation_in_meters")}

\item{overwrite_latlon_only_if_na}{If TRUE, only fill lat/lon when NA; else overwrite on match (default: FALSE)}

\item{overwrite_elev_only_if_na}{If TRUE, only fill elevation when NA; else overwrite on match (default: FALSE)}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
\code{records} with updated decimal_latitude, decimal_longitude, and (optionally) \code{elevation_col}.
Adds a helper column \code{time_diff_mins} indicating match gap (mins).
}
\description{
Geoencode records with GPX using per-record UTC offsets
}
