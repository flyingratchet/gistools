% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gistools.R
\name{build_coordinate_table}
\alias{build_coordinate_table}
\title{Build a unified, time-sorted tibble of GPX trackpoints}
\usage{
build_coordinate_table(
  path,
  glob = "*.gpx",
  lat_attr = "lat",
  lon_attr = "lon",
  ele_node = "ele",
  time_node = "time",
  out_lat = "decimal_latitude",
  out_lon = "decimal_longitude",
  out_ele = "elevation_in_meters",
  out_time = "date_time",
  tz = "UTC",
  quiet = TRUE,
  latlon_digits = 6,
  as_character = FALSE
)
}
\arguments{
\item{path}{Character. Parent folder containing GPX files.}

\item{glob}{File pattern. Default \code{"*.gpx"}.}

\item{lat_attr}{Attribute name for latitude on \verb{<trkpt>}. Default \code{"lat"}.}

\item{lon_attr}{Attribute name for longitude on \verb{<trkpt>}. Default \code{"lon"}.}

\item{ele_node}{Child node name (local-name) for elevation. Default \code{"ele"}.}

\item{time_node}{Child node name (local-name) for time. Default \code{"time"}.}

\item{out_lat, out_lon, out_ele, out_time}{Output column names.}

\item{tz}{Timezone for parsed times. Default \code{"UTC"}.}

\item{quiet}{Suppress non-fatal parse warnings. Default \code{TRUE}.}

\item{latlon_digits}{Integer digits to round lat/lon to. Set \code{NULL} to skip rounding. Default \code{6}.}

\item{as_character}{Logical: if \code{TRUE}, output lat/lon as fixed-width strings with trailing zeros
(using \code{latlon_digits}). Default \code{FALSE}.}
}
\value{
A tibble with columns named per \verb{out_*}. Elevation may be \code{NA}.
}
\description{
Recursively finds all .gpx files under \code{path}, parses \verb{<trkpt>} nodes,
and returns a tibble of latitude, longitude, optional elevation, and timestamps.
Namespace-safe (uses local-name()) and robust to empty/bad files.
}
